extends base.pug

block content
  header
    a.implicit(href='/')
      h1
        | District 61
  lspace
  div#main
    h0.red
      | РЕГИСТРАЦИЯ
    a(href='/login')
      | ВОЙТИ

    mspace

    form(method='post', action='')
      label(for='login')
        | Логин
      input(name='login', type='text')

      mspace

      label(for='email')
        | Почта
      input(name='email', type='text')

      mspace
      
      label(for='password')
        | Пароль
      input(name='password', type='password')

      mspace
      
      label(for='password')
        | Район
      select(name='district_id')
        each district in districts
          option(value=district.id)
            =district.name

      lspace

      input.primary-button.green(type='submit', value='ЗАРЕГИСТРИРОВАТЬСЯ')

      mspace

      h3.pixel.red
        =message

